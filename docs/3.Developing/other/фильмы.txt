Бегущий по лезвию 2049
24 часа на жизнь

Перевірити чи задане слово є паліндромом (метод Calculate має повернути булевське значення)
У тілі класу Exercise я створив статичний метод Calculate, який приймає рядок "word" та повертає булеве значення. У тілі методу Calculate я перевожу рядок "word" в нижній регістр за допомогою методу toLowerCase(). Потім я створив новий об'єкт класу StringBuilder, який міститиме переведений в нижній регістр рядок "word". Далі застосовуємо метод reverse() до об'єкту StringBuilder, щоб отримати його реверсовану копію. Використовуючи метод toString(), отримати рядок, що представляє реверсовану копію. Порівнюємо переведений в нижній регістр рядок word з його реверсованою копією та повертаємо результат порівняння. У тілі методу main викликаємо метод Calculate класу Exercise для трьох різних рядків та виводимо результати виклику на консоль за допомогою методу System.out.println()